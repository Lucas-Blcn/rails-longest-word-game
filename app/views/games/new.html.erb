
    <div class="d-flex justify-content-center align-items-center mb-2" style = "height: 100vh">
      <div class="text-center mt-4 py-5 border rounded-3 py-3 col-9 shadow-lg position-relative">
        <div class = "position-absolute top-0 start-0 p-2">
          <%= link_to "⬅ Go Back Home", root_path, class: "text-decoration-none text-secondary shadow-ms" %>
        </div>
        <h1>Let's Play!🔥</h1>
        <div class="d-flex align-items-center justify-content-center mt-4">
          <% @letters.each do |letter| %>
            <p class="p-3 border shadow-lg rounded-3 m-3 bg-primary text-white font-weight-bold" style="font-size: 28px">
              <%= letter %>
            </p>
          <% end %>
        </div>

        <!-- Placer le formulaire ici -->
        <div class="row justify-content-center">
          <div class = "col-7">
            <form action="/score" method="post" data-turbo="false" class="mt-4">
              <div class="input-group">
                <input type="text" class="form-control" name="mon_mot" placeholder="Choose letters to create the longest word">
                <input class="btn btn-primary" type="submit" id="button-addon2" value="See your score!">
              </div>
              <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
              <%= hidden_field_tag :letters, @letters %>
            </form>
          </div>
          </div>
        </div>
    </div>
